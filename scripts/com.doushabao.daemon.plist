<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>Label</key>
	<string>com.doushabao.daemon</string>

	<key>ProgramArguments</key>
	<array>
		<string>/usr/local/bin/node</string>
		<string>src/index.ts</string>
	</array>

	<!-- launchd starts with a minimal PATH, but cfg.dwsBin/piBin default to the
	     bare names "dws" and "pi". Without this the daemon starts and then
	     fails every spawn with ENOENT. install-launchd.sh overwrites this
	     with the installing user's real PATH. -->
	<key>EnvironmentVariables</key>
	<dict>
		<key>PATH</key>
		<string>/usr/local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
	</dict>

	<key>WorkingDirectory</key>
	<string>/opt/doushabao</string>

	<key>RunAtLoad</key>
	<true/>

	<key>KeepAlive</key>
	<true/>

	<key>StandardOutPath</key>
	<string>/opt/doushabao/var/daemon.log</string>

	<key>StandardErrorPath</key>
	<string>/opt/doushabao/var/daemon.log</string>
</dict>
</plist>
